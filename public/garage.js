class car {
  constructor(type, colour, maxSpeed, acceleration, friction, turnSpeed) {
    this.type = type;
    this.colour = colour;
    this.maxSpeed = maxSpeed;
    this.acceleration = acceleration;
    this.friction = friction;
    this.turnSpeed = turnSpeed;
  }
}
